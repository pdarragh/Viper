from viper.lexer import (
    NEWLINE as NL, INDENT as IND, DEDENT as DED, PERIOD, COMMA, COLON, OPEN_PAREN as OP, CLOSE_PAREN as CP, ARROW,
    ENDMARKER as EM,
    Number as Num,
    Name as N, ReservedName as RN,
    Class as C,
    Operator as Op,
)


lexemes = [
    RN('interface'), C('Shape'), COLON,
    NL, IND, RN('def'), N('get_area'), OP, CP, ARROW, C('Float'), DED,
    NL,
    NL, C('Shape'), C('Circle'), COLON,
    NL, IND, RN('def'), N('init'), OP, N('radius'), COLON, C('Int'), CP, COLON,
    NL, IND, IND, N('self'), PERIOD, N('radius'), COLON, C('Int'), Op('='), N('radius'), DED, DED,
    NL,
    NL, IND, RN('def'), N('get_area'), OP, CP, ARROW, C('Float'), COLON,
    NL, IND, IND, RN('return'), N('pi'), Op('*'), OP, N('self'), PERIOD, N('radius'), Op('^'), Num('2'), CP, DED, DED,
    NL,
    NL, C('Shape'), C('Quadrilateral'), COLON,
    NL, IND, RN('def'), N('init'), OP, N('length'), COLON, C('Int'), COMMA, N('width'), COLON, C('Int'), CP, COLON,
    NL, IND, IND, N('self'), PERIOD, N('length'), COLON, C('Int'), Op('='), N('length'),
    NL, IND, IND, N('self'), PERIOD, N('width'), COLON, C('Int'), Op('='), N('width'), DED, DED,
    NL,
    NL, IND, RN('def'), N('get_area'), OP, CP, ARROW, C('Float'), COLON,
    NL, IND, IND, RN('return'), N('self'), PERIOD, N('length'), Op('*'), N('self'), PERIOD, N('width'), DED, DED,
    NL,
    NL, C('Quadrilateral'), C('Rectangle'), COLON,
    NL, IND, RN('pass'), DED,
    NL,
    NL, C('Quadrilateral'), C('Square'), COLON,
    NL, IND, RN('def'), N('init'), OP, N('side'), COLON, C('Int'), CP, COLON,
    NL, IND, IND, N('self'), PERIOD, N('length'), COLON, C('Int'), Op('='), N('side'),
    NL, IND, IND, N('self'), PERIOD, N('width'), COLON, C('Int'), Op('='), N('side'), DED, DED,
    NL, EM,
]
